@model e_commerce_web_admin.ViewModels.Brands.BrandIndexViewModel
@{
    ViewData["Title"] = "Thương hiệu";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/brands.css" asp-append-version="true" />
}

@* ── Header ── *@
<div class="mb-6 flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 brand-anim" style="animation-delay:0.04s">
    <div>
        <div class="flex items-center gap-2 mb-1">
            <div class="w-8 h-8 rounded-xl bg-gradient-to-br from-teal-400 to-teal-600 flex items-center justify-center shadow-sm">
                <i data-lucide="tag" class="w-4 h-4 text-white"></i>
            </div>
            <h1 class="text-2xl font-bold text-slate-800">Thương hiệu</h1>
        </div>
        <p class="text-slate-500 text-sm">Quản lý danh sách thương hiệu sản phẩm</p>
    </div>
    <a asp-action="Create"
       class="inline-flex items-center gap-2 bg-gradient-to-r from-teal-500 to-teal-600 hover:from-teal-600 hover:to-teal-700
              text-white text-sm font-semibold px-5 py-2.5 rounded-xl transition-all shadow-sm hover:shadow-md hover:-translate-y-0.5">
        <i data-lucide="plus" class="w-4 h-4"></i>
        Thêm thương hiệu
    </a>
</div>

@* ── Toast messages ── *@
@if (TempData["Success"] != null)
{
    <div id="toastSuccess"
         class="mb-4 flex items-center gap-3 bg-emerald-50 border border-emerald-200 text-emerald-700 rounded-xl px-4 py-3 text-sm shadow-sm brand-anim"
         style="animation-delay:0.04s">
        <div class="w-7 h-7 rounded-full bg-emerald-100 flex items-center justify-center flex-shrink-0">
            <i data-lucide="check" class="w-4 h-4 text-emerald-600"></i>
        </div>
        <span class="flex-1">@TempData["Success"]</span>
        <button type="button" data-dismiss-target="toastSuccess" class="text-emerald-400 hover:text-emerald-600 transition-colors">
            <i data-lucide="x" class="w-4 h-4"></i>
        </button>
    </div>
}
@if (TempData["Error"] != null)
{
    <div id="toastError"
         class="mb-4 flex items-center gap-3 bg-red-50 border border-red-200 text-red-700 rounded-xl px-4 py-3 text-sm shadow-sm brand-anim"
         style="animation-delay:0.04s">
        <div class="w-7 h-7 rounded-full bg-red-100 flex items-center justify-center flex-shrink-0">
            <i data-lucide="alert-circle" class="w-4 h-4 text-red-500"></i>
        </div>
        <span class="flex-1">@TempData["Error"]</span>
        <button type="button" data-dismiss-target="toastError" class="text-red-400 hover:text-red-600 transition-colors">
            <i data-lucide="x" class="w-4 h-4"></i>
        </button>
    </div>
}

@* ── Stats cards ── *@
<div class="grid grid-cols-2 lg:grid-cols-4 gap-3 mb-5 brand-anim" style="animation-delay:0.08s">
    <div class="bg-white rounded-2xl border border-slate-100 p-4 flex items-center gap-3 shadow-sm">
        <div class="w-10 h-10 rounded-xl bg-teal-50 flex items-center justify-center flex-shrink-0">
            <i data-lucide="tag" class="w-5 h-5 text-teal-500"></i>
        </div>
        <div>
            <p class="text-xl font-bold text-slate-800">@Model.TotalCount</p>
            <p class="text-xs text-slate-400">Tổng thương hiệu</p>
        </div>
    </div>
    <div class="bg-white rounded-2xl border border-slate-100 p-4 flex items-center gap-3 shadow-sm">
        <div class="w-10 h-10 rounded-xl bg-emerald-50 flex items-center justify-center flex-shrink-0">
            <i data-lucide="check-circle-2" class="w-5 h-5 text-emerald-500"></i>
        </div>
        <div>
            <p class="text-xl font-bold text-emerald-600">@Model.ActiveCount</p>
            <p class="text-xs text-slate-400">Hoạt động</p>
        </div>
    </div>
    <div class="bg-white rounded-2xl border border-slate-100 p-4 flex items-center gap-3 shadow-sm">
        <div class="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center flex-shrink-0">
            <i data-lucide="eye-off" class="w-5 h-5 text-slate-400"></i>
        </div>
        <div>
            <p class="text-xl font-bold text-slate-500">@Model.InactiveCount</p>
            <p class="text-xs text-slate-400">Đã tắt</p>
        </div>
    </div>
    <div class="bg-white rounded-2xl border border-slate-100 p-4 flex items-center gap-3 shadow-sm">
        <div class="w-10 h-10 rounded-xl bg-teal-50 flex items-center justify-center flex-shrink-0">
            <i data-lucide="package" class="w-5 h-5 text-teal-500"></i>
        </div>
        <div>
            <p class="text-xl font-bold text-teal-600">@Model.TotalProductCount</p>
            <p class="text-xs text-slate-400">Sản phẩm</p>
        </div>
    </div>
</div>

@* ── Filter bar ── *@
<div class="bg-white rounded-2xl border border-slate-100 p-4 mb-4 shadow-sm brand-anim" style="animation-delay:0.12s">
    <form method="get" asp-action="Index" class="flex flex-col sm:flex-row gap-3">
        <div class="relative flex-1">
            <i data-lucide="search" class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none"></i>
            <input type="text" name="search" value="@Model.Search"
                   placeholder="Tìm theo tên hoặc slug..."
                   class="w-full pl-9 pr-4 py-2.5 border border-slate-200 rounded-xl text-sm text-slate-700
                          focus:outline-none focus:ring-2 focus:ring-sky-400 focus:border-transparent transition" />
        </div>
        <select name="status"
                class="border border-slate-200 rounded-xl px-4 py-2.5 text-sm text-slate-700 bg-white
                       focus:outline-none focus:ring-2 focus:ring-teal-400 focus:border-transparent transition">
            <option value="" selected="@(string.IsNullOrEmpty(Model.Status))">Tất cả</option>
            <option value="active"   selected="@(Model.Status == "active")">Đang hoạt động</option>
            <option value="inactive" selected="@(Model.Status == "inactive")">Đã tắt</option>
        </select>
        <button type="submit"
                class="inline-flex items-center justify-center gap-2 bg-teal-600 hover:bg-teal-700
                       text-white text-sm font-semibold px-5 py-2.5 rounded-xl transition-colors">
            <i data-lucide="search" class="w-4 h-4"></i>
            Tìm kiếm
        </button>
        @if (!string.IsNullOrEmpty(Model.Search) || !string.IsNullOrEmpty(Model.Status))
        {
            <a asp-action="Index"
               class="inline-flex items-center gap-1.5 text-slate-500 hover:text-slate-700 text-sm px-2 transition-colors">
                <i data-lucide="x-circle" class="w-4 h-4"></i>
                Xoá lọc
            </a>
        }
    </form>
</div>

@* ── Brand table ── *@
<div class="bg-white rounded-2xl border border-slate-100 overflow-hidden shadow-sm brand-anim" style="animation-delay:0.16s">

    @if (!Model.Brands.Any())
    {
        <div class="py-24 text-center">
            <div class="w-20 h-20 rounded-3xl bg-gradient-to-br from-teal-100 to-teal-50
                        flex items-center justify-center mx-auto mb-5 shadow-sm">
                <i data-lucide="tag" class="w-10 h-10 text-teal-300"></i>
            </div>
            <p class="text-slate-600 font-semibold text-lg">Không tìm thấy thương hiệu</p>
            <p class="text-slate-400 text-sm mt-2">
                @if (!string.IsNullOrEmpty(Model.Search))
                {<span>Thử từ khoá khác hoặc <a asp-action="Index" class="text-teal-600 hover:underline">xoá bộ lọc</a></span>}
                else
                {<span>Nhấn <strong class="text-slate-600">Thêm thương hiệu</strong> để bắt đầu</span>}
            </p>
        </div>
    }
    else
    {
        @* Table header *@
        <div class="px-6 py-3.5 bg-gradient-to-r from-slate-50 to-slate-50/50 border-b border-slate-100
                    grid grid-cols-[1fr_auto_auto_auto_auto] gap-4 items-center">
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-wider">Thương hiệu</span>
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-wider text-center w-28">Sản phẩm</span>
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-wider text-center w-28">Trạng thái</span>
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-wider text-center w-28">Ngày tạo</span>
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-wider text-right w-32">Thao tác</span>
        </div>

        @* Rows *@
        <div class="divide-y divide-slate-50">
            @{int animIdx = 0;}
            @foreach (var brand in Model.Brands)
            {
                <div class="brand-row grid grid-cols-[1fr_auto_auto_auto_auto] gap-4 items-center px-6 py-3.5 brand-anim"
                     style="animation-delay:@(0.16 + animIdx++ * 0.03)s"
                     data-id="@brand.Id">

                    @* Name + slug + logo *@
                    <div class="flex items-center gap-3 min-w-0">
                        <div class="brand-logo w-11 h-11 rounded-xl flex-shrink-0 border border-slate-100">
                            @if (!string.IsNullOrWhiteSpace(brand.ImagePath))
                            {
                                <img src="@brand.ImagePath" alt="@brand.Name" loading="lazy" decoding="async" />
                            }
                            else
                            {
                                <div class="w-full h-full flex items-center justify-center">
                                    <i data-lucide="image-off" class="w-4 h-4 text-slate-300"></i>
                                </div>
                            }
                        </div>
                        <div class="min-w-0">
                            <p class="font-semibold text-slate-800 text-sm truncate">@brand.Name</p>
                            <p class="text-[11px] text-slate-400 font-mono truncate">/brands/@brand.Slug</p>
                        </div>
                    </div>

                    @* Product count *@
                    <div class="w-28 text-center">
                        <span class="inline-flex items-center justify-center gap-1 text-xs font-semibold rounded-full px-2.5 py-1
                                     @(brand.ProductCount > 0 ? "bg-teal-100 text-teal-700" : "bg-slate-100 text-slate-400")">
                            <i data-lucide="package" class="w-3 h-3"></i>
                            @brand.ProductCount
                        </span>
                    </div>

                    @* Status toggle *@
                    <div class="w-28 text-center">
                        <button type="button"
                                data-brand-toggle
                                data-brand-id="@brand.Id"
                                class="badge-active inline-flex items-center gap-1.5 text-xs font-semibold
                                       px-3 py-1.5 rounded-full cursor-pointer transition-all duration-200
                                       @(brand.IsActive
                                           ? "bg-emerald-100 text-emerald-700 hover:bg-emerald-200"
                                           : "bg-slate-100 text-slate-500 hover:bg-slate-200")"
                                data-active="@brand.IsActive.ToString().ToLower()">
                            <span class="dot w-1.5 h-1.5 rounded-full flex-shrink-0
                                         @(brand.IsActive ? "bg-emerald-500" : "bg-slate-400")"></span>
                            <span class="status-label">@(brand.IsActive ? "Bật" : "Tắt")</span>
                        </button>
                    </div>

                    @* Created date *@
                    <div class="w-28 text-center">
                        <span class="text-xs text-slate-400">
                            @brand.CreatedAt.ToString("dd/MM/yyyy")
                        </span>
                    </div>

                    @* Actions *@
                    <div class="w-32 flex items-center justify-end gap-1.5">
                        <a asp-action="Edit" asp-route-id="@brand.Id"
                           title="Chỉnh sửa"
                           class="inline-flex items-center gap-1.5 text-xs font-medium
                                  bg-teal-50 hover:bg-teal-100 text-teal-600
                                  px-2.5 py-1.5 rounded-lg transition-colors">
                            <i data-lucide="pencil" class="w-3.5 h-3.5"></i>
                            Sửa
                        </a>

                        <form asp-action="Delete" asp-route-id="@brand.Id" method="post"
                              data-brand-delete
                              data-brand-name="@brand.Name"
                              data-product-count="@brand.ProductCount">
                            @Html.AntiForgeryToken()
                            <button type="submit"
                                    title="Xoá"
                                    class="inline-flex items-center gap-1.5 text-xs font-medium
                                           bg-red-50 hover:bg-red-100 text-red-500
                                           px-2.5 py-1.5 rounded-lg transition-colors">
                                <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                Xoá
                            </button>
                        </form>
                    </div>
                </div>
            }
        </div>

        @* Pagination *@
        @if (Model.TotalPages > 1)
        {
            <div class="px-6 py-4 border-t border-slate-100 flex items-center justify-between bg-slate-50/50">
                <p class="text-xs text-slate-400">
                    Trang <span class="font-semibold text-slate-600">@Model.Page</span> / @Model.TotalPages
                    &nbsp;·&nbsp;
                    <span class="font-semibold text-slate-600">@Model.TotalCount</span> thương hiệu
                </p>
                <div class="flex items-center gap-1">
                    @if (Model.HasPrev)
                    {
                        <a asp-action="Index" asp-route-page="@(Model.Page - 1)"
                           asp-route-search="@Model.Search" asp-route-status="@Model.Status"
                           class="w-8 h-8 rounded-lg border border-slate-200 bg-white hover:bg-slate-50
                                  flex items-center justify-center text-slate-600 transition-colors shadow-sm">
                            <i data-lucide="chevron-left" class="w-4 h-4"></i>
                        </a>
                    }
                    @for (int p = Math.Max(1, Model.Page - 2); p <= Math.Min(Model.TotalPages, Model.Page + 2); p++)
                    {
                        <a asp-action="Index" asp-route-page="@p"
                           asp-route-search="@Model.Search" asp-route-status="@Model.Status"
                           class="w-8 h-8 rounded-lg text-xs font-semibold flex items-center justify-center transition-all
                                  @(p == Model.Page
                                      ? "bg-gradient-to-b from-teal-500 to-teal-600 text-white shadow-sm"
                                      : "border border-slate-200 bg-white text-slate-600 hover:bg-slate-50")">
                            @p
                        </a>
                    }
                    @if (Model.HasNext)
                    {
                        <a asp-action="Index" asp-route-page="@(Model.Page + 1)"
                           asp-route-search="@Model.Search" asp-route-status="@Model.Status"
                           class="w-8 h-8 rounded-lg border border-slate-200 bg-white hover:bg-slate-50
                                  flex items-center justify-center text-slate-600 transition-colors shadow-sm">
                            <i data-lucide="chevron-right" class="w-4 h-4"></i>
                        </a>
                    }
                </div>
            </div>
        }
    }
</div>

@section Scripts {
    <script src="~/js/brands.js" asp-append-version="true"></script>
}
